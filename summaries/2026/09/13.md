# Activity Summary for 9/13/2026

## 12:26:02 AM
The file `/home/athos/Projects/ubi_compass/app.py`, last modified on 9/12/2026 at 10:27:34 PM, outlines a Flask web application named "Compass". This application serves as an intermediary for managing and pushing menu item data, primarily from CSV files, to an external "Solum" system.

Key functionalities include:

*   **Flask Application Core:** Sets up a Flask server with various routes for both frontend rendering and API interactions.
*   **CSV Data Management:**
    *   Expects specific CSV headers for menu items (`Menu`, `Meal`, `label_id`, `Menu Item - EN`, `Vegetarian`, `Vegan`, etc.).
    *   Parses uploaded CSV files, validates headers, and stores the data in memory (`LAST_UPLOAD`) using a `threading.Lock` for thread safety.
    *   A default `compass_template.csv` is loaded at startup.
*   **API Endpoints:**
    *   `/api/config`: Provides application configuration, including its name ("Compass") and API base URL.
    *   `/api/options`: Returns metadata about the currently loaded CSV, such as file name, row count, and hierarchical lists of menus and meals.
    *   `/api/upload`: Handles CSV file uploads, parsing and validating the content before updating the in-memory data.
    *   `/api/launch`: Initiates the process of pushing selected menu and meal items to the Solum system. It transforms CSV rows into a structured "article" format expected by Solum.
*   **Solum Integration:**
    *   The `push_to_solum` function manages sending articles to a Solum endpoint. It handles authentication by obtaining an access token and sends articles in batches of 1000.
    *   A significant addition is the `assign_labels_to_solum` function, which is called after articles are uploaded. This new function groups articles by their `label_id` and then uses the Solum API's `/common/api/v2/common/labels/link` endpoint to assign these labels to the respective articles, enhancing the integration to include label management.
*   **CORS Handling:** Dynamically configures `Access-Control-Allow-Origin` headers based on an environment variable (`PUBLIC_CORS_ORIGINS`) and specific hardcoded domains (`.github.io`, `.pages.dev`).
*   **Data Transformation:** Includes functions like `icon_filename` to generate icon names based on boolean flags (e.g., Vegetarian, Vegan) and `to_article` to map CSV data to the Solum article structure.
*   **Static File Serving:** Provides routes for serving the `index.html` template, the `compass_template.csv`, and image assets (`ubi_logo.jpeg`, `logo_top.png`).
*   **Configuration:** Relies heavily on environment variables for API base URLs, CORS origins, and all Solum system credentials (endpoint, username, password, company, store).

The most notable update within this code version is the explicit implementation of label assignment functionality for the Solum system. This is achieved through the introduction of the `assign_labels_to_solum` function and its integration into the `push_to_solum` workflow, indicating an enhanced capability to link articles with specific labels in Solum after initial data loading.

Recurring patterns include: extensive use of environment variables for configuration, strict CSV data validation, Flask routing for both data and UI, and a consistent approach to interacting with the external Solum API involving token-based authentication and batch processing.