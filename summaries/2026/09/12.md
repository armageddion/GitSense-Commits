# Activity Summary for 9/12/2026

## 11:26:03 PM
The `app.py` file, last modified on 9/12/2026 at 10:27:34 PM, outlines a Flask application named "Compass" designed to process and integrate menu-related data, primarily from CSV files, with an external system called "Solum".

Key updates and functionalities include:

*   **CSV Processing and Validation:** The application defines `EXPECTED_HEADERS` to enforce a strict schema for uploaded CSV files, which include details like "Menu," "Meal," "label_id," and various nutritional flags (e.g., "Vegetarian," "Vegan," "Halal"). The `parse_csv` function handles reading, validation, and error reporting for missing columns or empty data. A default template (`compass_template.csv`) is loaded at startup.
*   **Data Management:** A `LAST_UPLOAD` dictionary stores the most recently processed CSV data (filename, headers, rows), protected by a `DATA_LOCK` for thread-safe access.
*   **API Endpoints:**
    *   `/api/config`: Provides application configuration, including `appName` and `apiBaseUrl`.
    *   `/api/options`: Returns metadata about the currently loaded data, such as the file name, row count, and sorted lists of unique menus and meals.
    *   `/api/upload`: Accepts CSV file uploads, parses them, validates headers, and updates the `LAST_UPLOAD` data.
    *   `/api/launch`: Takes a selected "menu" and "meal" from the loaded data, transforms the corresponding rows into "articles," and pushes them to the Solum system.
*   **Solum Integration:** The `push_to_solum` function handles authentication with the Solum API, sending articles in batches, and optionally linking labels to articles via the `assign_labels_to_solum` function. This integration relies heavily on environment variables for configuration (endpoint, user, password, company, store).
*   **Data Transformation:** The `to_article` function maps CSV row data to a specific JSON structure required by Solum, including generating an `Icon` filename based on the nutritional flag columns using `icon_filename`.
*   **Frontend Support:** Routes like `/`, `/template`, `/logo`, and `/logo-top` serve static assets (`index.html`, `compass_template.csv`, `ubi_logo.jpeg`, `logo_top.png`) to support a web interface.
*   **CORS Handling:** An `add_public_cors_headers` hook dynamically sets CORS headers based on allowed origins configured via environment variables or specific domains (`.github.io`, `.pages.dev`).

Recurring elements in the content include the reliance on environment variables for various configurations (public API base URL, CORS origins, Solum credentials), the structured approach to CSV parsing and validation against a predefined schema, and the transformation of internal data into a format suitable for an external API. Error handling is present across several API endpoints for robust operation.