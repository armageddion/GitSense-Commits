# Activity Summary for 8/28/2026

## 2:47:09 PM
The log exclusively tracks changes within the file `/home/athos/Projects/ubi_compass/app.py`.

**Summary of Changes:**

*   **Initial Version (8/28/2026, 1:48:29 PM):**
    *   The `app.py` file implements a Flask web application designed to manage and push menu item data, likely to a Solum digital signage system.
    *   It defines a `Flask` application (`app`), a `DATA_LOCK` for thread safety, and a `LAST_UPLOAD` dictionary to store CSV data in memory.
    *   Key functionalities include:
        *   `parse_csv`: Reads CSV content, validates against `EXPECTED_HEADERS`, strips whitespace, and filters empty rows. Raises `ValueError` for missing headers or no data.
        *   `icon_filename`: Generates an icon filename (e.g., "OOOOOOOOOXOOOOXO.png") based on "X" or "O" values in `FLAG_COLUMNS` (Vegetarian, Vegan, No Alcohol, No Dairy, No Gluten, No Nuts, Halal).
        *   `to_article`: Transforms a CSV row into a structured dictionary ("article") suitable for the Solum API, including `articleId`, `articleName`, and `data` fields with various categories and the generated icon filename.
        *   `selected_rows`: Filters the `LAST_UPLOAD` data based on "Menu" and "Meal" criteria.
        *   `push_to_solum`: Handles authentication with the Solum API using credentials from environment variables, retrieves an access token, and then uploads articles in batches of 1000. It includes error handling for network requests and API responses.
    *   API Endpoints:
        *   `/`: Renders `index.html`.
        *   `/template`: Serves `compass_template.csv` for download.
        *   `/api/options`: Returns JSON containing the uploaded filename, row count, and sorted lists of menus and meals from the current data.
        *   `/api/upload`: Accepts a CSV file, parses it using `parse_csv`, and updates the `LAST_UPLOAD` data. Returns options or error messages.
        *   `/api/launch`: Accepts a `menu` and `meal`, filters rows, converts them to articles, and pushes them to Solum via `push_to_solum`. Returns the count of items, the items themselves, and the Solum API result.
    *   The application uses environment variables for Solum API credentials and the `PORT` number.

*   **Minor Log Entries (8/28/2026, 2:11:52 PM, 2:12:42 PM, 2:13:04 PM):**
    *   No functional code changes were observed in `app.py` at these timestamps. The content is identical to the initial version. These likely represent intermediate saves or non-substantive changes.

*   **Significant Update (8/28/2026, 2:42:31 PM):**
    *   A new function `load_default_template()` was added. This function reads and parses the `compass_template.csv` file from the application's directory and uses its content to initialize the `LAST_UPLOAD` dictionary.
    *   A direct call to `load_default_template()` was added immediately after its definition, ensuring that default CSV data is loaded into memory when the application starts up.
    *   A new GET route `/logo` was introduced. This route serves the `ubi_logo.jpeg` file as an image (mimetype="image/jpeg").

*   **Final State (8/28/2026, 2:42:43 PM):**
    *   No further code changes were made to `app.py` after the 2:42:31 PM update. The content is identical to the previous entry.

**Patterns and Recurring Elements:**

*   **Single File Focus:** All logged changes pertain to the `app.py` file, indicating focused development on this core application logic.
*   **Frequent Saves:** The timestamps show multiple entries for the same file in quick succession (e.g., within seconds or minutes) with identical content, suggesting frequent saves without significant code alterations between them.
*   **Flask Application Structure:** The application consistently follows a standard Flask structure with routes, request handling, and `jsonify` for API responses.
*   **CSV Processing and Solum Integration:** The core functionality revolves around parsing CSV data, transforming it for a specific external API (Solum), and pushing that data, with consistent use of `EXPECTED_HEADERS` and `FLAG_COLUMNS` for data interpretation.
*   **Environment Variable Dependence:** Critical configuration details like Solum API endpoints, credentials, company/store names, and the application port are consistently sourced from environment variables, promoting flexible deployment.
*   **Thread Safety:** The `DATA_LOCK` is consistently used to protect shared `LAST_UPLOAD` data, indicating consideration for concurrency.